function [P] = mda_mixture_density (params,mda,load)
% Compute recall density assuming full MDA model
% FORMAT [P] = mda_mixture_density (params,mda,load)
% 
% params        model parameters (see mda_lat2par.m)
% mda           model data structure
% load          data
% P             density over recall attribute

% Compute cued density given slots (eq. 24)

% Compute posterior distribution over items given cue (Eq 26-27)

% Compute recall density given slots (eq. 24)

% Compute recall density